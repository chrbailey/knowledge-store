[project]
name = "knowledge-store"
version = "0.1.0"
description = "SQLite + FTS5 searchable knowledge base for Claude Code insights"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
